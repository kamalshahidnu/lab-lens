[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
norecursedirs =
  model_development/scripts
  model_development/notebooks
  notebooks
  airflow
  models
  logs

markers =
  e2e: end-to-end smoke tests (require deployed service URLs via env)

