"""
Helper scripts package.

Note: modules under `scripts/` are used by CLI utilities and a small number of tests.
Keep imports lightweight (avoid importing large ML deps at module import time).
"""


